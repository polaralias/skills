interface:
  display_name: "Query to Knowledge"
  short_description: Resolve repository ambiguity into canonical OKF-compatible knowledge.
  default_prompt: "Use $query-to-knowledge to resolve repository questions from evidence and focussed judgement, then capture the answers in the established canonical format with explicit last-meaningful-change timestamps for changed OKF concepts, citations, indexes, and producer-safe derived-knowledge reconciliation."
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg

policy:
  allow_implicit_invocation: true
  products:
  - chatgpt
  - codex
  - api
  - atlas

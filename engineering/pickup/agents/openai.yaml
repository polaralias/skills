interface:
  display_name: "Pickup"
  short_description: "Resume work from a saved local handoff."
  default_prompt: "Use $pickup to resume work from the latest local handoff and current canonical docs."

policy:
  allow_implicit_invocation: true

interface:
  display_name: implementation-plan-writer
  short_description: Produce a customer-facing implementation plan from kickoff material,
    local guidance, and confirmed delivery...
  default_prompt: 'Use implementation-plan-writer when the task matches this skill:
    Produce a customer-facing implementation plan from kickoff material, local guidance,
    and confirmed delivery assumptions. Use when a user needs a structured onboarding
    or implementation plan that carries a customer from setup through go-live without
    expanding into generic project-management overhead.'
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  allow_implicit_invocation: true
  products:
  - chatgpt
  - codex
  - api
  - atlas

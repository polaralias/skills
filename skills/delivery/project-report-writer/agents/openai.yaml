interface:
  display_name: Project Report Writer
  short_description: Build project reports from the freshest credible delivery signals
    available, then validate them against structured...
  default_prompt: 'Use Project Report Writer when the task matches this skill: Build
    project reports from the freshest credible delivery signals available, then validate
    them against structured execution data and durable project context. Use when the
    user wants a status report, health view, launch-readiness picture, risk summary,
    milestone review, or similar project reporting output.'
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  allow_implicit_invocation: true
  products:
  - chatgpt
  - codex
  - api
  - atlas

interface:
  display_name: "Query to Knowledge"
  short_description: Resolve repository ambiguity into canonical OKF-compatible knowledge.
  default_prompt: Use $query-to-knowledge to resolve repository questions from evidence and focused judgment, then capture the answers in the established canonical format, including OKF concepts, citations, indexes, and producer-safe OpenWiki reconciliation when present.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg

policy:
  allow_implicit_invocation: true
  products:
  - chatgpt
  - codex
  - api
  - atlas

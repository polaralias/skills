interface:
  display_name: local-pickup
  short_description: Resume work from a saved local handoff and re-verify the current state.
  default_prompt: Use local-pickup to resume work from the most relevant local handoff, re-check the current canonical docs and git state, and restate the next verified action.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg

policy:
  allow_implicit_invocation: true
  products:
  - chatgpt
  - codex
  - api
  - atlas

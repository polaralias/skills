interface:
  display_name: local-pickup
  short_description: Resume from one active handoff, excluding superseded and rechecking expired context.
  default_prompt: Use local-pickup to resume from the active handoff for the matching workstream, exclude superseded or misplaced candidates, re-verify expired context against canonical docs and git state, identify duplicate-active lifecycle drift, and restate the next verified action.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg

policy:
  allow_implicit_invocation: true
  products:
  - chatgpt
  - codex
  - api
  - atlas

---
type: Visualization
title: "{{title}}"
description: "{{description}}"
tags: [{{tags}}]
timestamp: "{{timestamp}}"
authority: "{{authority}}"
verification: "{{verification}}"
source: "{{source}}"
renderer: "{{renderer}}"
output: "{{output}}"
temporal_basis: timestamp
history_model: current-records-only
drift_policy: linked-source-newer-than-target-review-signal
exclusions: []
---

# {{title}}

## Purpose

Describe the question this view answers and its intended audience.

## Source and generation

- Canonical source: [{{source}}]({{source}})
- Renderer: `{{renderer}}`
- Derived output: [{{output}}]({{output}})
- Exclusions: list any bundle-relative paths or globs deliberately omitted from the derived view, or state `none`. Directory-name entries ending in `/` match at every depth; use them for nested dependency folders such as `node_modules/` and `.venv/`.

The source records remain authoritative. Regenerate the output through the named renderer; do not hand-edit the derived artefact. For standalone OKF HTML, verify that pinned runtimes are embedded and that opening or navigating the output makes no runtime network requests.

## Reading the view

Explain the visual encoding, filters, known omissions, and any accessibility alternative. For dense graphs, verify that semantic zoom preserves labelled entry-point, foundational, and highly connected landmarks, while selecting a record produces a readable, non-overlapping neighbourhood focus. Exclusions scope only the derived view; they do not change concept authority, connectivity, or conformance.

## Temporal reading and drift

Explain which declared event field drives temporal ordering. Unless retained historical concepts or versions exist, state that the view orders current records and does not reconstruct earlier fact content.

Treat newer-linked-source versus older-target ordering as a possible drift review signal only. Record the evidence needed before changing either concept.

## Verification

Record the generation or smoke check that supports the current `verification` value.

interface:
  display_name: repo-dissection
  short_description: Dissect an unclear repository and turn it into documented, verified understanding.
  default_prompt: Use repo-dissection to map the repository, verify what is actually true in docs, code, and runtime, classify the mismatches, and bootstrap a usable documentation foundation.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg

policy:
  allow_implicit_invocation: true
  products:
  - chatgpt
  - codex
  - api
  - atlas

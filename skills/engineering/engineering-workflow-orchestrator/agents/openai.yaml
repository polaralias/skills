interface:
  display_name: "Engineering Workflow Orchestrator"
  short_description: "Coordinate engineering workflow, branch topology, and session closure."
  default_prompt: "Use $engineering-workflow-orchestrator to classify the current stage, distinguish parallel worktrees from dependency-stacked Git delivery, route to the narrowest specialist, reconcile active coordination manifests before repo-session-alignment closure, and keep each state surface within its ownership boundary."
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg

policy:
  allow_implicit_invocation: true
  products:
  - chatgpt
  - codex
  - api
  - atlas

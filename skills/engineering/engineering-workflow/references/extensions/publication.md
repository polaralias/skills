# Publication Extension

Use only when public-release hardening is explicitly in scope. Establish the active truth surface, verify real implementation and packaging paths, remove stale public surfaces deliberately, and run bounded secret, PII, local-path, environment, and cache scans. Add release automation only when the repository publication model requires it. Resolving `publication-safety` establishes readiness evidence; publication still requires explicit external authority.

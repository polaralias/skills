# QA Planning Extension

Use when the accepted contract requires a human-reviewable QA plan beyond implementation TDD. Preserve requirement-to-test traceability and distinguish planning coverage from executable test evidence. Escalate missing product truth instead of inventing expected behaviour. Resolve `qa-coverage` only when the plan covers the accepted requirements and names intentional omissions.

# Tracker Synchronisation Extension

Use only for stable, already-designed task records with an explicit destination, mapping, direction, authority, and conflict policy. Preserve repository identity separately from provider identity. Inspect egress, retain unknown local fields, detect remote revision conflicts, and read back writes before advancing the binding base. Fall back to tracker-ready output when live access or authority is unavailable. Never redesign hierarchy or acceptance during publication.

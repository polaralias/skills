# Request authentication

The GitHub credential gateway reads the provider registry and hydrates request headers before the API client sends a request.

# Execution records

The task ledger owns delivery status and acceptance evidence. It does not own architectural decisions.

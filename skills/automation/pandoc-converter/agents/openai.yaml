interface:
  display_name: Pandoc Converter
  short_description: Run Pandoc conversions through a small wrapper that preserves
    predictable defaults while still allowing arbitrary...
  default_prompt: 'Use Pandoc Converter when the task matches this skill: Run Pandoc
    conversions through a small wrapper that preserves predictable defaults while
    still allowing arbitrary Pandoc flags. Use when converting between markup or document
    formats and when you want `--wrap=none` unless a different wrap mode is requested.'
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  allow_implicit_invocation: true
  products:
  - chatgpt
  - codex
  - api
  - atlas

interface:
  display_name: pandoc-converter
  short_description: Run Pandoc conversions with predictable defaults and guarded executable filters.
  default_prompt: 'Use pandoc-converter when the task matches this skill: Run Pandoc
    conversions through a small wrapper that preserves predictable defaults, forwards
    ordinary Pandoc flags, and requires explicit opt-in for executable filters. Use
    when converting between markup or document formats and when you want `--wrap=none`
    unless a different wrap mode is requested.'
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  allow_implicit_invocation: true
  products:
  - chatgpt
  - codex
  - api
  - atlas
